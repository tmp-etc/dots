header --> ExecCommand

supports a Command Center feature which allows executing OS-level commands via HTTP endpoints.



CVE-2024-50603
--------------------
string evaluated as shell command
grep for:
	function exec_command()
	cmdstr variables
	escapeshellarg() missing on the cmdstr values or even the use of
	checks like `if ($sid && $sid != "undefined")`, where only the presence of vars is checked



CVE-2024-55591
--------------------
grep for:
	_getAdminSession
	getValidatedSession
	_getSession
	... and the like and review the if-else logic



CVE-2025-0108
--------------------
Web request to the management interface is handled by three separate components: Nginx, Apache, and the PHP application itself.

Authentication is set at the Nginx level and based on HTTP headers, which we can set almost arbitrarily. Our request is then re-processed again in Apache, which may process the path or
headers differently to Nginx! Then and only then is the request handed down to PHP.



CVE-2024-48248
--------------------
File file = new File(newPath);
return FileUtils.readFileToByteArray(file);



CVE-2025-24893
--------------------
The root cause is unsafe evaluation of Groovy content inside the SolrSearch macro, i.e adding user data to the template and then handing it over to the engine.



CVE-2024-53704
--------------------
Whilst parsing cookie strings: There is no else clause after the if statement, so if the loop runs into a null character (NB! this mainly applies to C) in the cookie string, it simply skips validation and jumps to returning the session ID. Assuming that it was possible to input null bytes in a base64-encoded cookie, we were confident that we had found the authentication bypass!



CVE-2025-24016
--------------------
grep for eval. and trace from there

tho:
The Wazuh server API must be accessible to the attacker, typically over the internet. This is a setup we strongly discourage.
The attacker must have valid administrator-level API credentials, typically obtained by credential theft, default passwords, or poor security practices.



CVE-2025-1661
--------------------
$template = $this->options['template'];
$path = $this->get_ext_path() . "views/templates/{$template}.php"; //templates inside the plugin

so user input from http requests get just concated/interpolated to form the path



CVE-2025-47812
--------------------
- look for serialization entrypoints
- and using nullbytes for filters



CVE-2025-32432
--------------------
grep for something that `extends \yii\base\Model` and allows user input to flow into it

cuz that has some weird behaviour

"So to summarise, using the as x syntax, we’re able to attach a behavior (aka a mixin) to a Component."

and also just Yii's stuff and ask LLM's to give you the sinks yadayada



CVE-2025-29927
--------------------
grep for funny custom headers x-middleware-subrequest and other x-somethingsomething



CVE-2024-46506
--------------------
misuse of legit behavior; endpoint for that was in a js file and i guess assumed not to be directly poked by a user. i guess grep -i for `cmd` and go from there



CVE-2025-4427
--------------------
- A request hits the controller with an attacker-controlled format parameter.
- Spring MVC binds query parameters to DeviceFeatureUsageReportQueryRequest
- @Valid triggers DeviceFeatureUsageReportQueryRequestValidator.isValid().
- The validator calls localizedMessageBuilder, inserting the untrusted format value into a message template.
- The template is parsed by the EL engine; any ${…} expression is evaluated immediately. 
- Only after validation finishes does MethodSecurityInterceptor execute the @PreAuthorize check which is obviously too late.
- Result: arbitrary code runs in the application context, no credentials required.

so user input gets put in a 'error' template and this one gets put through a templating engine without sanitization


CVE-2025-1974
--------------------



CVE-2025-47916
--------------------
"This method passes the value of the content parameter to the Theme::makeProcessFunction() method; hence it is evaluated by the template engine. " -- sooo... trace functions that invoke a template engine and then trace input to those functions
